Painted backgrounds go here as `<room-id>.png` (320x200). `npm run art:process` writes them;
the game picks up any file in this folder automatically and falls back to the code-drawn
scene for rooms without one. See ART.md in the repo root.
